[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py loaddata fixtures/all_universities_data.json && python manage.py collectstatic --noinput && gunicorn config.wsgi --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
